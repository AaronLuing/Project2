<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head.ejs') %>
</head>
<body>
    <nav class="navigation">
        <h1 class="navH1">MüvieChat</h1>
            <% if (currentUser) { %>
                <div class="login">
                    Welcome <%= currentUser.username %>
                    <div class="icon" id="nava">
                        <img src="<%= currentUser.icon %>">
                    </div>
                        <form id="nava" action="/session?_method=DELETE" method="POST">
                            <input type="submit" value="Log Out" class="btn-small red" />
                        </form>
                </div>
                <% } else { %>
                <div class="login">
                    <a href="/user/new" id="nava">Sign Up</a><br>
                <a href="/session/new" id="nava">Log In</a>
                </div>
        <% } %>
    </nav>
    <a href="/show">Click Here for Movies</a>
    <a href="/new">Add a Movie</a>
    <form action="/search">
        <input type="text" name="title" placeholder="Find a movie">
        <input type="submit" value="Search">
    </form> 
    <div>
        <% for(let i=0; i<movies.length; i++){ %>
            <div>
                <h4><%= movies[i].title %> </h4>
                <a href="/<%= movies[i].id %>"><img src="<%= movies[i].poster %>"></a>
            </div>
        <% } %>
    </div>
    
</body>
</html>